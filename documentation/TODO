1. Run architector to document/check each of the four layers in a separate doc each
2. Scene re-upload on every dirty frame re-generates *all* geometry CPU-side and re-uploads to the GPU, which will not scale beyond small models.
3. Learn more about O(V) and o(V)
4. Understand pathway to testing - what tests to use, how to create, if anything needs refactoring for testability
5. Consider some sort of shading in the future